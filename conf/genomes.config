/*
 * -------------------------------------------------
 *  Nextflow config file for reference genomes
 * -------------------------------------------------
 * Defines reference genomes, without using iGenome paths
 * Can be used by any config that customizes the base
 * path using $params.refDir / --refDir
 * stolen from nf-core-rnafusion, thanks guys!
 */

params {
  references {
    'genome' {
      fa      = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/bwa/${params.vepGenome}_${params.vepVersion}.fa"
      fai     = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/bwa/${params.vepGenome}_${params.vepVersion}.fa.fai"
      dict    = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/bwa/${params.vepGenome}_${params.vepVersion}.dict"
      bwa     = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/bwa/"
      star    = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/star_*"
      saf     = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/saf/${params.vepGenome}_${params.vepVersion}.saf"
      intlist = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/intlist/${params.vepGenome}_${params.vepVersion}.interval_list"
      refflat = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/refflat/${params.vepGenome}_${params.vepVersion}.refflat"
      rrna    = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/rrna/${params.vepGenome}_${params.vepVersion}.rRNA.interval_list"
      vcf     = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/vcf"
      vep     = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/.vep"
      exome   = "${params.refDir}/${params.vepGenome}_${params.vepVersion}/exome"
    }
  }
}
